services:
  catalogdb:
    container_name: catalogdb
    image: postgres:17  # or pin to a specific version you want
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=admin123
      - POSTGRES_DB=CatalogDB
    restart: always
    ports:
      - "5432:5432"
    volumes:
      - postgres_catalog:/var/lib/postgresql

volumes:
  postgres_catalog: